Phase 2 数学分析
10个sync用户，每次请求阻塞3秒：

理论最大吞吐 = 10 ÷ 3s = 3.33 req/s
实测 2.88 req/s（理论值的86%，网络开销正常）
60秒内sync只处理了 173个订单

async同样10个用户，每次请求仅~25ms：

实测 29.5 req/s → 60秒内处理 1775个订单
吞吐量是sync的 10.3倍

sync每秒"丢失"的订单数：如果真实需求是30 req/s，sync只能接受6.67 req/s，每秒白白丢掉约 23个订单。